# Sleep Assistant Chatbot - Python Backend Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
flask==3.0.0
flask-cors==4.0.0
numpy==1.26.2

# Optional: For better semantic search (recommended)
sentence-transformers==2.2.2

# Optional: For GPT-powered responses (requires API key)
openai==1.3.0

# Optional: For better JSON handling
ujson==5.8.0
